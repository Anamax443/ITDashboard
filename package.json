{
  "name": "itdashboard",
  "private": true,
  "version": "0.0.1",
  "description": "Domain & PC admin dashboard — AD insight, eventlog analytics, script runner.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
