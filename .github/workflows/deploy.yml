name: Deploy to 10.8.2.213

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, windows, itdashboard-prod]
    timeout-minutes: 15
    defaults:
      run:
        shell: cmd
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps
        run: npm install --workspace @itdashboard/server --include-workspace-root

      - name: Typecheck
        working-directory: apps/server
        run: npm run typecheck

      - name: Build
        working-directory: apps/server
        run: npm run build

      - name: Apply DB migrations
        working-directory: apps/server
        env:
          SQL_HOST: ${{ vars.SQL_HOST }}
          SQL_INSTANCE: ${{ vars.SQL_INSTANCE }}
          SQL_DATABASE: ${{ vars.SQL_DATABASE }}
          SQL_TRUSTED_CONNECTION: 'true'
        run: npm run migrate

      - name: Restart API Windows Service
        run: |
          net stop ITDashboardAPI
          net start ITDashboardAPI
          sc query ITDashboardAPI
